## 로그래머스 
### 즐겨찾기가 가장 많은 식당 정보 출력하기
```SQL
WITH TEMP AS(
SELECT FOOD_TYPE,
        REST_ID,
        REST_NAME,
    SUM(FAVORITES) AS FAVORITES,
        RANK() OVER(PARTITION BY FOOD_TYPE ORDER BY SUM(FAVORITES) DESC) AS RNK        
FROM REST_INFO 
GROUP BY FOOD_TYPE, REST_ID, REST_NAME)

SELECT FOOD_TYPE,
        REST_ID,
        REST_NAME,
        FAVORITES
FROM TEMP 
WHERE RNK = 1 
ORDER BY FOOD_TYPE DESC 
```
